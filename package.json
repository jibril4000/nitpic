{
  "name": "nitpic",
  "private": true,
  "version": "0.2.0",
  "description": "Browser feedback → Claude Code bridge (Chrome extension + Claude Code plugin)",
  "workspaces": [
    "shared",
    "daemon",
    "extension"
  ],
  "scripts": {
    "build": "npm run build -w @nitpic/shared && npm run build -w @nitpic/daemon && npm run build -w @nitpic/extension",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
