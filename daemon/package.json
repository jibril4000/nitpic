{
  "name": "@nitpic/daemon",
  "version": "0.2.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs"
  },
  "dependencies": {
    "@nitpic/shared": "0.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "esbuild": "^0.24.0"
  }
}
